apiVersion: v1
kind: ConfigMap
metadata:
  name: aws-auth
  namespace: kube-system
data:
  mapUsers: |
    - userarn: arn:aws:iam::316412036948:user/github-actions-ecr-eks
      username: github-actions
      groups:
        - system:masters
